"""MultiEmu application package.

This package holds the user-facing CLI/orchestration layer. Core emulator
components remain in their existing top-level packages such as ``machines``,
``frontend`` and ``cpu``.
"""
